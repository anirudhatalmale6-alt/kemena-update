import React from 'react';
import { View } from 'react-native';

// AdMob banner - will be configured after client provides ad unit IDs
const AdBanner = () => {
  return <View />;
};

export default AdBanner;
